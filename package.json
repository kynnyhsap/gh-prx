{
  "name": "gh-prx",
  "version": "0.1.5",
  "description": "GitHub CLI extension for agentic PR and CI workflows",
  "keywords": [
    "agent",
    "ci",
    "cli",
    "gh",
    "github",
    "pr"
  ],
  "homepage": "https://github.com/kynnyhsap/gh-prx#readme",
  "bugs": {
    "url": "https://github.com/kynnyhsap/gh-prx/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kynnyhsap/gh-prx.git"
  },
  "bin": {
    "gh-prx": "./gh-prx"
  },
  "files": [
    "gh-prx",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "build:check": "bun run build && git diff --exit-code -- dist",
    "build:exe": "bun scripts/build-exe.ts",
    "build:release": "bun scripts/build-release.ts",
    "release:cut": "bun scripts/release-cut.ts",
    "typecheck": "bunx tsgo --noEmit -p tsconfig.json",
    "lint": "bunx oxlint . --ignore-pattern \"**/dist/**\" --ignore-pattern \"**/release-dist/**\"",
    "format": "bunx oxfmt .",
    "test": "bun test",
    "e2e:live": "bash scripts/e2e-live.sh",
    "e2e:mutate": "GH_PRX_E2E_MUTATE=1 bun test test/e2e/live-mutate.test.ts",
    "prepublishOnly": "bun run build && bun run typecheck && bun run lint"
  },
  "dependencies": {
    "goke": "latest",
    "picocolors": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "latest",
    "oxfmt": "latest",
    "oxlint": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
